package entity.building.base;

import gui.BoardCell;

public class SingleTargetTower extends Tower {
    public SingleTargetTower(BoardCell cell, int turretSprite) {
        super(cell, turretSprite);
    }
}
